var die = Math.floor(Math.random()*6 +1);
var score;
if (die === 1) {
  score = 0;
  console.log("your rolled "+die+" for a score of " +score)
} else {
  score = die;
  console.log("your rolled "+die+" for a score of " +score)
}
